# OpticSpy Transmitter

This demo uses code from the "opticspy" demo, to provide a shell interface over USB serial (ACM).
The interface allows setting the Tomu's red and green LEDs to various brightness/blink values.
Functionality is implemented using Timer PWM.